name: Mediplus Project Platform - GCP Deployment
on: push

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: First Step Of Deployment
        run: echo "Starting the GCP Deployment Process"

      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Second Step Of Deployment
        run: echo "Finalizing the GCP Deployment Process"
